# Diário — 29/08/2026

## 5.1 Estratégias da semana

| Estratégia | Nome | Contexto | Motivo de troca |
|---|---|---|---|
| S1 | Serial + núcleo matemático | Implementação do mapeamento pixel→complexo, iteração z=z²+c, normalização 0-255, e loop serial completo com escrita do `.pgm` e medição de tempo | — (ponto de partida) |
| S2 | Paralelização OpenMP | Reuso do núcleo matemático (S1), paralelização do loop duplo com `#pragma omp parallel for num_threads(...)` | Necessário comparar tempo serial vs paralelo, exigido pelo enunciado |
| S3 | Pthreads — divisão estática por blocos | Divisão da imagem em blocos contíguos de linhas, um bloco por thread, criação manual via `pthread_create`/`pthread_join` | Enunciado exige controle manual de threads (Pthreads), diferente da abstração do OpenMP |
| S4 | Pthreads — fila dinâmica com mutex | Contador global `proxima_linha` protegido por `pthread_mutex_t`, cada thread pega uma linha por vez até acabar o trabalho | Enunciado exige uma segunda estratégia de divisão distinta da S3; fila dinâmica permite balanceamento de carga entre threads |

## 5.2 Diário de Tentativas

| Nº | Estratégia | O que foi tentado | Resultado | Hipótese/causa de falha | Evidência |
|---|---|---|---|---|---|
| 1 | S1 | Compilar `main.c` isoladamente após separar núcleo matemático em `mandelbrot.c`/`mandelbrot.h` | Falhou: `undefined reference to iterar` | Makefile não incluía `mandelbrot.c` na regra de compilação, só como dependência | `evidencias.log` |
| 2 | S1 | Teste isolado da função `iterar` com pontos calculados manualmente (`c=0`, `c=1`) | Sucesso: `iterar(0,0,100)=100`, `iterar(1,0,100)=3`, ambos batendo com a conta manual | — | `evidencias.log` |
| 3 | S4 | Primeira tentativa: criar uma thread nova a cada linha dentro de um `while`, em vez de N threads persistentes puxando trabalho de uma fila | Não compilava (variável de índice `i` inexistente) e lógica invertida: contador local incrementado em vez do contador global compartilhado | Confusão entre o papel da thread principal (criar/orquestrar) e o papel de cada thread trabalhadora (consumir fila em loop próprio) | Revisão de código, sem execução |
| 4 | S4 | Correção: `argumentos_fila` com ponteiros para contador e mutex compartilhados; `calcular_fila` como função de thread com loop próprio consumindo o contador | Sucesso: saída idêntica ao serial via `diff` | — | `evidencias.log` |
| 5 | S1–S4 | Comparação da saída (`6x6`, `max_iteracoes=30`) com gabarito fornecido pelo professor | Divergência: gabarito não apresenta a simetria vertical esperada matematicamente (conjugados `(real, imag)`/`(real, -imag)` devem ter o mesmo número de iterações) | Testada com 3 implementações independentes (núcleo próprio, versão com `complex.h`, e verificação do intervalo alternativo proposto) — todas convergem para o mesmo resultado, diferente do gabarito. Hipótese: gabarito não corresponde aos parâmetros de entrada informados, ou há erro de transcrição | Scripts de teste isolados (não incluídos no entregável) |
| 6 | S1–S4 | Testes de robustez: argumentos ausentes/excedentes, valores não numéricos, negativos, zero | Todos bloqueados corretamente com mensagem de erro coerente em stderr | — | `evidencias.log` |
| 7 | S1–S4 | Teste de caso extremo: `largura=1` e `altura=1` | Inicialmente causaria divisão por zero em `pixel_para_complexo` (denominador `largura-1`/`altura-1` = 0); corrigido com checagem `if (largura > 1)` | Caso não previsto no enunciado, mas plausível em correção automatizada | `evidencias.log` |
| 8 | S1–S4 | Verificação de consistência entre as 4 implementações (`diff` byte a byte) em dois tamanhos diferentes (`300x300` e `50x50`, threads variando) | Sucesso: todos os pares idênticos | — | `evidencias.log` |

## Notas para seções futuras do relatório

- **Decisões técnicas**: separação do núcleo matemático (`pixel_para_complexo`, `iterar`, `normalizar`, `escrever_pgm`, `escrever_tempo`) em `mandelbrot.c`/`.h`, reaproveitado pelas 4 implementações — evita duplicação e garante saída idêntica por construção.
- **Limitações**: teste de "falha na criação do arquivo de saída" via permissão de diretório (`chmod 000`) é arriscado (pode travar acesso ao próprio diretório) — validado apenas por revisão de código, não por execução direta.
- **Uso de IA**: apoio consultivo (explicações conceituais, revisão de código, sugestão de testes) via Claude, sem geração do código-fonte da implementação.
